{
  "name": "server",
  "version": "1.0.0",
  "description": "SSIM backend",
  "main": "server.js",
  "scripts": {
    "client-install": "cd ../ssim && npm install",
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "cd ../ssim && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false cd ../ssim && npm install && npm run build"
  },
  "author": "Laybson",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^5.0.2",
    "express": "^4.17.1",
    "mongoose": "^5.8.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
